eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false  # Set to false if this IS the discovery server
    fetch-registry: false        # Set to false if this IS the discovery server
  instance:
    hostname: localhost
    prefer-ip-address: true
